[project]
name = "worker"
version = "0.1.0"
description = "Genesis Background Worker"
requires-python = ">=3.10"
dependencies = [
    "nats-py>=2.7.2",
    "genesis_db",
]

[tool.uv.sources]
genesis_db = { workspace = true }
