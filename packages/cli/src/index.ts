/**
 * Genesis CLI — Entry Point
 *
 * This is the main entry point for the `genesis` CLI.
 * Commands are registered here and dispatched to their handlers.
 *
 * Phase 0.2: Stub only — no implementation.
 * Phase 0.3+: Wire up command handlers using a CLI framework (e.g., commander, oclif).
 *
 * Usage (future):
 *   genesis setup      — First-time environment setup
 *   genesis start      — Start all platform services
 *   genesis stop       — Stop all platform services
 *   genesis doctor     — Run health checks
 *   genesis config     — View/edit platform configuration
 *   genesis models     — Manage AI models
 *   genesis plugins    — Manage plugins
 *   genesis workspace  — Manage workspaces
 */

// TODO(phase-0.3): Import and register command handlers
// import { setupCommand } from './commands/setup.js';
// import { startCommand } from './commands/start.js';
// import { stopCommand } from './commands/stop.js';
// import { doctorCommand } from './commands/doctor.js';
// import { configCommand } from './commands/config.js';
// import { modelsCommand } from './commands/models.js';
// import { pluginsCommand } from './commands/plugins.js';
// import { workspaceCommand } from './commands/workspace.js';

console.log('Genesis CLI — stub (Phase 0.2)');
console.log('Full implementation coming in Phase 0.3');
